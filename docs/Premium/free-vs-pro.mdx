---
title: "Plans"
description: "What's included in Free, Starter, Pro, and Pro+"
sidebarTitle: "Plans"
---

Vektra is free to use for any server. **Starter** adds branding and customization, **Pro** unlocks the full A&R workflow and dashboard, and **Pro+** currently has the same bot features as Pro while deeper white-label hosting is prepared.

<Info>
  Vektra keeps each server's slash command list tier-aware. Free servers see the core commands, Starter servers see Starter tools, and Pro or Pro+ servers see the full workflow commands after premium is redeemed.
</Info>

## At a glance

| Feature | Free | Starter | Pro | Pro+ |
| --- | --- | --- | --- | --- |
| Demo submissions via `/submit` | Yes | Yes | Yes | Yes |
| Staff submission cards with Approve, Reject, and DM | Yes | Yes | Yes | Yes |
| Private staff threads per submission | Yes | Yes | Yes | Yes |
| Submission queue, recent, and panel views | Yes | Yes | Yes | Yes |
| Status updates with `/status` | Yes | Yes | Yes | Yes |
| Artist commands and leaderboard | Yes | Yes | Yes | Yes |
| Managed storage | Yes | Yes | Yes | Yes |
| Release requests via `/make-release` and `/check-releases` | Yes | Yes | Yes | Yes |
| Release distribution payout | 90% to label / 10% service fee | 100% to label | 100% to label | 100% to label |
| DM reply forwarding from artists | No | Yes | Yes | Yes |
| Custom branding | No | Yes | Yes | Yes |
| Custom submit panel | No | Yes | Yes | Yes |
| Custom approval and rejection DM templates | No | Yes | Yes | Yes |
| Custom form prompt | No | Yes | Yes | Yes |
| Cooldowns, duplicate rules, and submission limits | No | Yes | Yes | Yes |
| Approved and rejected routing channels | No | Yes | Yes | Yes |
| Footer, logo, and success-message customization | No | Yes | Yes | Yes |
| Choose storage region with `/storage` | No | No | Yes | Yes |
| Bring your own Neon database with `/setup_db` | No | No | Yes | Yes |
| Dashboard login and server picker | No | No | Yes | Yes |
| Review submissions from the dashboard | No | No | Yes | Yes |
| Review support tickets from the dashboard | No | No | Yes | Yes |
| Submit and track release requests from the dashboard | No | No | Yes | Yes |
| Dashboard analytics graphs and CSV export | No | No | Yes | Yes |
| Staff notes | No | No | Yes | Yes |
| Reviewer assignment | No | No | Yes | Yes |
| Shortlist tools | No | No | Yes | Yes |
| Quick status buttons | No | No | Yes | Yes |
| Priority flags | No | No | Yes | Yes |
| Demo ratings | No | No | Yes | Yes |
| Saved rejection reasons | No | No | Yes | Yes |
| Weekly digest | No | No | Yes | Yes |
| Analytics | No | No | Yes | Yes |
| CSV export | No | No | Yes | Yes |
| Support tickets | No | No | Yes | Yes |

## Release distribution fee

Free servers can still submit release requests for Vektra QC and Labelcaster delivery. On the free tier, Vektra keeps a **10% service fee**, so the label keeps **90%**.

Paid tiers keep **100%** of distribution earnings.

<Info>
  The service fee only applies to release distribution handled through Vektra. It does not affect normal demo submissions.
</Info>

## Starter features

### Branding

Make Vektra feel closer to your label.

- `/brand` opens a form to set your display name, required submit panel caption, and embed color.
- `/brand_info` shows current branding and premium state.
- `/brand_clear` resets branding to defaults.

`/brand` also tries to update the bot's server nickname when permissions allow it.

<Info>
  Discord does not let a shared bot use a different avatar or online status per server. Starter branding applies to server-specific messages, embeds, panels, and the bot's server nickname where permissions allow it.
</Info>

### Submission experience

- `/post_panel` posts a branded public submit button panel.
- `/form label:<text> placeholder:<text>` customizes the optional submission question.
- `/templates` sets custom approval and rejection DM templates. Either template can be edited alone.
- `/extras footer_text:<text> logo_url:<url or none> success_message:<text>` customizes footers, thumbnails, and submitter confirmation.
- `/limits cooldown_minutes:<minutes> max_submissions_per_user:<count> duplicate_policy:<policy>` controls cooldowns, caps, and duplicate handling.
- `/routing approved_channel:<channel> rejected_channel:<channel>` routes accepted and rejected updates.

## Pro features

- The web dashboard lets staff review submissions and tickets, submit and track release requests, approve or reject demos, DM users, export CSV files, and view analytics graphs.
- `/note ticket_id:<id> note:<text>` adds private staff notes.
- `/reviewer ticket_id:<id> reviewer:<member>` assigns a reviewer.
- `/shortlist ticket_id:<id> enabled:<true or false>` adds or removes from the shortlist.
- `/shortlisted` views the shortlist.
- Pro submission cards include quick buttons for Review, Shortlist, Processed, Contacted, and Signed.
- `/priority ticket_id:<id> enabled:<true or false>` marks or unmarks priority.
- `/rate ticket_id:<id> score:<1-10>` stores an internal rating.
- `/reasons reasons:<reason, reason>` saves rejection reasons.
- `/digest channel:<channel>` posts a weekly submission digest and saves the digest channel.
- `/analytics` shows Pro submission analytics.
- `/export` exports submissions to CSV.
- `/storage region:<region>` moves managed storage to West US, Europe (UK), or South-East Asia.
- `/setup_db` connects your own Neon database.
- Support ticket commands: `/ticket_channel`, `/ticket_panel`, `/tickets`, and `/ticket_set`.

<Note>
  Pro+ currently unlocks the same features as Pro. It exists now so buyers can be tracked separately before future white-label hosting is added.
</Note>

## Dashboard

The dashboard uses Discord login. After login, Vektra only shows servers where:

- You are a server admin or have Manage Server permission.
- Vektra is installed in that server.
- The server has an active Pro or Pro+ tier for dashboard-only tools.

From the dashboard you can edit branding, templates, storage, ticket settings, submission statuses, ticket statuses, release requests, and post panels into channels the bot can see.

<Warning>
  When posting panels from the dashboard, make sure the bot can view the selected channel and has Send Messages permission there.
</Warning>

## Storage changes

When you change storage region or connect a custom database, Vektra migrates existing submissions, tickets, branding, and Pro settings before switching. Old managed storage is cleaned up after a successful move.

Pro and Pro+ servers can also change storage from the dashboard. Use this when you want to move from one custom Neon database to another without asking staff to run commands in Discord.

## How to upgrade

```text
/premium
```

Premium access is handled manually:

1. Run `/premium` to see contact and pricing info.
2. Contact the owner and pay.
3. Receive a redeemable coupon code.
4. In your server, run `/redeem code:<your-coupon>`.
5. Check status anytime with `/pro_status`.

<Warning>
  When premium expires, saved paid settings are kept but inactive. Existing branded submit panels and ticket panels may still be visible in Discord, but paid workflows stop opening until the server renews.
</Warning>
